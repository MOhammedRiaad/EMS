# Test Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=ems_user
POSTGRES_PASSWORD=ems_secret
POSTGRES_DB=ems_studio_test
DATABASE_URL=postgresql://ems_user:ems_secret@127.0.0.1:5433/ems_studio_test

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=test-secret-key
JWT_EXPIRES_IN=7d

# App
NODE_ENV=test
PORT=3001

# Storage (disabled for tests)
MINIO_ENDPOINT=http://localhost:9000
MINIO_ROOT_USER=minio_user
MINIO_ROOT_PASSWORD=minio_secret
MINIO_BUCKET=ems-files

# Mail (disabled for tests)
SMTP_HOST=localhost
SMTP_PORT=1025
